const router = require("express").Router();

router.route("/order")
    //Get all orders
    .get(() => {

    })
    //Create an order
    .post(() => {

    })
    //Edit an order (So that the users can edit out stuff while the order is pending.)
    .put(() => {

    })
    //Remove an order
    .delete(() =>{

    })